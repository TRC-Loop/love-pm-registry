# .love_packagem Spec

This is basically a .zip file, with another extension.

Structure:

```
.
├── prop.json
└── src/
    └── <code files>
```

`prop.json` example:

```
{
    "name": "love-pm-test",
    "version": 1.0
}
```

